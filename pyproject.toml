[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "funder-pipeline"
version = "0.1.0"
description = "Research funder ETL pipeline"

[project.scripts]
fp = "funder_pipeline.main:main"

[tool.setuptools.packages.find]
where = ["src"]