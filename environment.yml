name: Funder_API
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pandas
  - selenium
  - pip
  - pip:
      - webdriver-manager


# to create the virtual envrinment 
#conda env create -f environment.yml

# To activate this environment, use
#
#     $ conda activate Funder_API
#
# To deactivate an active environment, use
#
#     $ conda deactivate